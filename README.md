# 🪷 Radha Krishna Python Sketch (ASCII Art Generator)

A simple Python script that converts any image (such as Radha Krishna) into detailed ASCII text art.

## 📋 Requirements

This project requires:

- Python 3.x
- Pillow (PIL) library

### Install Pillow

Open your terminal and run:

```bash
pip install pillow
